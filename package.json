{
	"name": "consumer-demo",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Example consumer that requests a verification, prints the URL, and shows the decrypted+verified result.",
	"scripts": {
		"start": "node --env-file-if-exists=.env --experimental-strip-types src/index.ts",
		"setup": "node --env-file-if-exists=.env --experimental-strip-types src/setup.ts",
		"check": "tsc --noEmit"
	},
	"dependencies": {
		"@reclaimprotocol/client": "file:../../builder/packages/client",
		"express": "^5.1.0"
	},
	"devDependencies": {
		"@types/express": "^5.0.0",
		"typescript": "^5.9.0"
	}
}
